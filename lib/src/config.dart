const gameHeight = 1080.0;
const gameWidth = 1920.0;
const runnerWidth = gameWidth * 0.2;
const runnerHeight = gameHeight * 0.2;
const obstacleHeight = gameHeight * 0.2;
const obstacleWidth = gameWidth * 0.2;

const ballRadius = gameWidth * 0.02;
const batWidth = gameWidth * 0.2;
const batHeight = ballRadius * 2;
const batStep = gameWidth * 0.05;

const runnerStep = gameWidth / 3;
